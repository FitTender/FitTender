import * as React from 'react';
import {View} from 'react-native';
import BackTitle from '../../common/BackTitle/BackTitle';

const Header = () => {
  return <View></View>;
};

export default React.memo(Header);
